export interface Idesarrolladora {
    idDesarrolladora?: string;  // ID opcional (para Firestore)
    nombre: string;
    descripcion: string;
    fechaFundacion: string;
    logo: string;  // URL del logo
  }
  